Deletes an amenity.
---
tags:
  -  "amenities"
produces:
  - "application/json"
parameters:
  - in: path
    name: amenity_id
    description: "The ID of the amenity to delete."
    required: true
    schema:
      type: "string"

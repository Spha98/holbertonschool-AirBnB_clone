Deletes a state.
---
tags:
  -  "states"
produces:
  - "application/json"
parameters:
  - in: path
    name: state_id
    description: "The ID of the state to delete."
    required: true
    schema:
      type: "string"
responses:


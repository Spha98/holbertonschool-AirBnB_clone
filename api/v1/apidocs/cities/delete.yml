Deletes a city.
---
tags:
  -  "cities"
produces:
  - "application/json"
parameters:
  - in: path
    name: city_id
    description: "The ID of the city to delete."
    required: true
    schema:
      type: "string"
responses:


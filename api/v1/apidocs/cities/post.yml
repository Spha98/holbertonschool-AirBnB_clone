Creates a new city.
---
tags:
  -  "cities"
consumes:
  - "application/json"
produces:
  - "application/json"
parameters:
  - in: path
    name: state_id
    description: "The ID of the related state."
    required: true
    schema:
      type: string
  - in: query
    name: name
    description: "The name of the new city."
    required: true
    schema:
      type: string
responses:
 

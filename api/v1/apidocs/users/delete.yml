Deletes a user.
---
tags:
  -  "users"
produces:
  - "application/json"
parameters:
  - in: path
    name: user_id
    description: "The ID of the user to delete."
    required: true
    schema:
      type: string
responses:

